server:
  port: 8081

spring:
  datasource:
    #url: jdbc:h2://localhost:8082/review;
    url: jdbc:h2:tcp://localhost/~/review;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
